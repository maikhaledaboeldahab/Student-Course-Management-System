# Student-Course-Management-System
C++ console application for managing students and course enrollments. Applies OOP (inheritance, encapsulation), STL containers (vector, set), and STL algorithms (sort, find_if).
